/*
    Demo on Conditional or ternary operation:

    (expression) ? op1 : op2;
*/


#include <stdio.h>

int main()
{
    int x = 10, y=20, z;
    z = (1) ? x:y;
    printf("\nz=%d\n", z);
    return 0;
}